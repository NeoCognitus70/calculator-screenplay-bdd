# Calculator Screenplay BDD

This project teaches BDD-style test automation with Playwright, TypeScript, and
the sibling `hand-baked-screenplay-pattern` package.

The implementation will grow in small, documented commits so each layer can be
studied independently: project setup, calculator domain, REST API, UI,
Screenplay test model, and verification documentation.
